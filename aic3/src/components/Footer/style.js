import styled from "styled-components";

export const Container = styled.footer`
  margin-top: auto;
  padding: 0.75rem;
  background-color: var(--white);

  p {
    color: var(--black);
    text-align: center;

    strong {
      color: var(--primary);
    }
  }
`;
